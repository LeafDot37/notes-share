# 优化程序性能

# 整体

优化源程序主要在更快和更正确上下手

程序性能并不仅仅是时间复杂度，相同时间复杂度的性能会有很大不同，首先要关注常数因子，同时针对编译器优化也是常用思路，当然针对编译器优化会有局限性：

在基本约束条件下运行
▪ 不能引起程序行为的任何改变
▪ 通常阻止进行那些只影响病态条件下行为的优化

大多数分析都是基于静态信息的

当有疑问时，编译器必须是保守的

# 普遍有用的优化

这部分指对于大多数代码都可以应用的优化方式，也即编译器应该做的优化

## 代码移动

根本思路是减少计算执行的频率，如将循环中的计算如果不会受到循环次数的影响则可以移动到循环外部进行一次运算

![1777001706284](images/5/1777001706284.png)

## 复杂运算简化

用更简单的运算替换昂贵的操作，比如用移位，加，替代乘除

![1777001793527](images/5/1777001793527.png)

## 共享公用子表达式

重用表达式或表达式的一部分

![1777001867580](images/5/1777001867580.png)

# 妨碍优化的因素

## 函数调用

![1777208447031](images/5/1777208447031.png)

看起来可以通过将strlen提出循环，防止strlen重复执行，但是实际上这并不可行

![1777208510638](images/5/1777208510638.png)

因此如果在循环中调用函数，并不能简单地将其直接提出循环外来做简化，如果简单来说，就是某些函数单次调用和多次调用会产生除返回结果以外的变化

## 内存别名使用

![1777208522033](images/5/1777208522033.png)

简单来说，就是两个指针指向同一个位置，这样在更新时会产生不同的结果

![1777209213178](images/5/1777209213178.png)

对于这种情况，如果a和b指向同一个地址，那么在循环中赋值b[i]与在外边一次性更新b[i]会有不同的效果

![1777210656032](images/5/1777210656032.png)

# 表示程序性能

![1777218519329](images/5/1777218519329.png)

## CPE

![1777219785556](images/5/1777219785556.png)

简单来说，CPE就是曲线的斜率，用来表示程序的性能

## 例子

![1777293196328](images/5/1777293196328.png)

![1777293207222](images/5/1777293207222.png)

有这么一个向量，对其做加法和乘法运算，测试程序性能

![1777293251290](images/5/1777293251290.png)

首先，可以将vec_length移到循环外边，这样就可以减少调用该函数的次数，进而提高性能

![1777293454818](images/5/1777293454818.png)

其次，通过将get_vec_start函数移动到循环外，用地址访存，也可以减少调用函数的时间

![1777293419077](images/5/1777293419077.png)

但是单纯将get_vec_start移动到外边效果并不是非常好，还可以进一步引入局部变量。对于原来的操作，会进行多次访问和写入内存*dest，而通过引入局部变量，将值先暂时存放在寄存器中，循环结束后再整个写入内存，这样可以大大提高性能

# 现代处理器

## 基本架构

前面的优化都是基于程序编译，通过减少函数调用以及不必要内存开销来优化性能，但是想要进一步优化，需要考虑处理器的微体系结构

指令级并行就是现代处理器架构最重要的提升

![1777385188654](images/5/1777385188654.png)

这样的架构允许处理器一个周期执行多条指令，且是乱序执行，允许执行顺序与原始程序顺序不一致

分支单元用来检测预测是否正确，在确认之前，选择分支与不选择分支二者并行执行。

退役单元用来检测一条指令是否完成，当一条指令结束后，同时相关的分支均为正确的选择，那么就判断该指令已经完成，将指令从队列中退役，并更新数值

![1777219996652](images/5/1777219996652.png)

## 功能单元性能

![1777385335403](images/5/1777385335403.png)

![1777221149210](images/5/1777221149210.png)

![1777385719432](images/5/1777385719432.png)

延迟、发射、容量，延迟表示运算完成所需要的总时间（时钟周期为单位），发射表示两次运算之间间隔的最小周期数，容量表示执行该运算的处理器的数量

![1777735564964](images/5/1777735564964.png)

![1777735737258](images/5/1777735737258.png)

延迟界限指的是系统处理单个任务所需时间的理论极限。它回答的问题是：“完成一个操作，最快需要多久？”延迟界限，简单来说就是完成这个操作所需要的最小CPE值。延迟界限仅考虑单条操作

吞吐量界限指的是系统在单位时间内能够处理的任务总量的理论极限。它回答的问题是：“一秒钟内，最多能处理多少任务？取倒数变成CPE即为吞吐量界限”吞吐量界限，给出CPE的最小界限。吞吐量界限考虑了并行，是整个系统的能力

例如，因为只有一个整数乘法器，它的发射时间为 1 个时钟周期 ，处理器不可能支持每个时钟周期大于 1 条乘法的速度。另一方面 ，四个功能单元都可以执行整数加法，处理器就有可能持续每个周期执行 4 个操作的速率。不 幸的是，因 为需要从内存读数据，这造成了另一个吞吐量界限。两个加载单元限制了处理器每个时钟周期最多只能读取两个数据值，从而使得吞吐量界限为 0.50。我们会展示延迟界限和吞吐量界限对合并函数不同版本的影响。

## 数据流图

用来描述数据走向进而展示数据依赖关系

![1777736080848](images/5/1777736080848.png)

对于形成循环的代码片段，我们可以将访问到的寄存器分为四类：

只读 ：这些寄存器只用作源值，可以作为数据 ，也可以用来计算内存地址，但是在循环中它们是不会被修改的。循环 combine 的只读寄存器是%rax。

只写：这些寄存器作为数据传送操作的目的。在本循环中没有这样的寄存器。

局部：这些寄存器在循环内部被修改 和使用，迭代与迭代之间不相关。在这个循环中，条件码寄存器就是例子：cmp 操作会修改它们，然后 jne 操作会使用它们，不过这种相关是在单次迭代之内的。

循环：对于循环来说，这些寄存器既作为源值，又作为目的 ，一次迭代中产生的值会在另一次迭代中用到。可以看到，%rdx和％xmm0 是 combine4的循环寄存器，对应于程序值 data+i 和acc。

循环寄存器之间的操作链决定了限制性能的数据相关

![1777736545681](images/5/1777736545681.png)

![1777736619465](images/5/1777736619465.png)

对于 combined 为什么我们获得了等于 5 个周期延迟界限的CPE。当执行这个函数时，浮点乘法器成为了制约资源。循环中需要的其他操作——控制和测试指针值 data+i以及从内存中读数据——与乘法器并行地进行。每次后继的acc的值被计算出来，它就反馈回来计算下一个值，不过只有等到 5 个周期后才能完成。

当然关键路径的acc只是提供了下界，真正测试中还会有其他的影响因素

# 循环展开

## 2×1展开

![1777221251841](images/5/1777221251841.png)

![1777221381111](images/5/1777221381111.png)

![1777738148455](images/5/1777738148455.png)

2×1循环展开优化的部分本质上是得益于减少了循环开销操作，即指针+2过程中1 次“判断”、1 次“跳转”，但是并不能超过延迟界限，这是因为其本质上并没有利用多个计算单元，仍然存在数据依赖，受延迟界限的影响，要想打破延迟界限，就要尝试实现并行，在每个循环周期中采用多个计算单元

## 2×2展开

![1777221403060](images/5/1777221403060.png)

![1777738310436](images/5/1777738310436.png)

![1777221507334](images/5/1777221507334.png)

对于L×K循环展开，由于L<=K，于是随着L和K的不断增加，会不断趋近于吞吐量界限，而K为1时，随着L不断增加，会不断趋近于延迟界限

![1777738718554](images/5/1777738718554.png)

## 重组计算

![1777738846530](images/5/1777738846530.png)

![1777738869543](images/5/1777738869543.png)

![1777738908817](images/5/1777738908817.png)

![1777738926890](images/5/1777738926890.png)

简单来说，

第一步：取出 `a` **和** `b`，计算 `temp = a + b`

第二步：取出 `sum` **和** `temp`，计算 `new_sum = sum + temp`

这两步可以形成类似流水线的工作机制，即第一步执行时，上一个循环的第二步也在同步执行，进而形成并行的现象

## 向量并行

![1777740909324](images/5/1777740909324.png)

![1777740920509](images/5/1777740920509.png)

# 分支处理

![1777221546855](images/5/1777221546855.png)

![1777221565112](images/5/1777221565112.png)

![1777221627317](images/5/1777221627317.png)

![1777221634051](images/5/1777221634051.png)

![1777221643701](images/5/1777221643701.png)

![1777221664414](images/5/1777221664414.png)

# 内存性能

加载性能和存储性能（读写性能）共同组成了内存的性能

在读写过程中会出现读写相关的情况，即如果读和写的内存地址都相同，那么必须要先等写结束后再读，这会影响并行效率。

相对于运算过程，内存操作需要拿到地址指向后才能确定下一步操作，而不能和运算过程一样简单预测

# 实例

![1777221719813](images/5/1777221719813.png)

![1777221727802](images/5/1777221727802.png)
