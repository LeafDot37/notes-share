---
created: 2026-07-20
tags: [it-ch4, info-theory, block-coding]
---

# 块编码

块编码（block coding）是将信源输出序列按固定长度 $n$ 分块，对每个块整体进行编码的方法。与逐个符号编码不同，块编码的核心思想是通过扩展信源（即考虑信源的 $n$ 次扩展），打破单个符号整数码长的限制，通过将概率进一步细分来减少因取整造成的额外开销。

对单个符号进行编码时，由于码长必须为整数，平均码长 $\bar{L}$ 与信源熵 $H(X)$ 之间最多存在 1 bit 的差距。但当对 $n$ 个符号组成的块进行编码时，每个符号的平均码长可以满足：

$$
H(X) \leq \frac{1}{n}\bar{L}_n < H(X) + \frac{1}{n}
$$

其中 $\bar{L}_n$ 是 $n$ 长序列块编码的总平均码长。随着块长度 $n$ 的增大，取整造成的额外开销 $1/n$ 可以无限趋近于零。这背后的直观原因是：将多个符号捆绑在一起后，原本被截断的概率细节得以保留——长序列的概率分布比单个符号的概率分布有更多可能的概率值，从而让整数码长的取整误差被分摊到多个符号上。例如，若某符号概率 $p = 0.3$，其单符号最优整数码长为 2（因为 $-\log_2 0.3 \approx 1.74$），浪费了约 0.26 bit；但将两个符号捆绑后，序列 $(0.3, 0.3)$ 的概率为 0.09，其最优码长为 $\lceil -\log_2 0.09 \rceil = 4$，每个符号仅浪费约 0.09 bit。

在实际应用中，块编码面临计算复杂度随块长 $n$ 指数增长的问题。当 $n$ 较大时，可能的信源序列数量 $|\mathcal{X}|^n$ 极其庞大，直接为每个序列分配码字在计算上不可行。因此实际系统中往往采用算术编码（arithmetic coding）或 Lempel-Ziv 算法来近似实现块编码的效果，在保持较高压缩率的同时避免指数级增长的复杂度。

## 链接到

- 上一个知识点：[4.8 定长编码](4.8%20%E5%AE%9A%E9%95%BF%E7%BC%96%E7%A0%81.md)
- 下一个知识点：[4.10 符号编码](4.10%20%E7%AC%A6%E5%8F%B7%E7%BC%96%E7%A0%81.md)
