---
created: 2026-07-20
tags: [ss-ch5, signals-and-system, group-delay]
---

# 群时延与相位失真

## 群时延的定义

群时延（Group Delay）是描述系统对信号包络延迟的重要物理量。对于相位特性为 $\phi(\omega)$ 的系统，群时延定义为相位对角频率的导数的负值：

$$
\tau(\omega) = -\frac{d\phi(\omega)}{d\omega}
$$

其中 $\phi(\omega)$ 是系统频率响应 $H(\omega) = |H(\omega)| e^{j\phi(\omega)}$ 的相位函数。

### 与相时延的区别

相时延（Phase Delay）定义为：

$$
\tau_p(\omega) = -\frac{\phi(\omega)}{\omega}
$$

相时延衡量的是单个正弦波分量通过系统后的时间延迟，而群时延衡量的是信号包络（多个频率分量组成的"群"）通过系统的延迟。两者在一般情况下不相等，只有在 $\phi(\omega)$ 为过原点的直线时，二者才相等。

## 群时延的物理意义

### 窄带信号的群延迟

考虑一个窄带调制信号 $x(t) = a(t) \cos(\omega_0 t)$，其中 $a(t)$ 为缓慢变化的包络。该信号通过相位特性为 $\phi(\omega)$ 的系统后，输出信号的包络延迟近似为 $\tau(\omega_0)$。

数学解释：窄带信号在频域集中在 $\omega_0$ 附近，对相位函数进行一阶泰勒展开：

$$
\phi(\omega) \approx \phi(\omega_0) + \phi'(\omega_0)(\omega - \omega_0)
$$

代入系统输出表达式可得，包络 $a(t)$ 的延迟为 $-\phi'(\omega_0) = \tau(\omega_0)$，而载波 $\cos(\omega_0 t)$ 的延迟为 $-\phi(\omega_0)/\omega_0 = \tau_p(\omega_0)$。

### 群时延的直观理解

群时延的直观含义是**信号能量传播的时延**：

- 对于调幅信号，群时延决定了调制包络（即信息内容）的传输延迟
- 对于脉冲信号，群时延决定了脉冲峰值位置的时移
- 对于通信系统，群时延的平坦度直接影响到符号间干扰（ISI）的程度

## 群时延与失真

### 群时延常数——无失真传输

当群时延为常数时，系统对所有频率分量的包络施加相同的延迟时间：

$$
\tau(\omega) = \tau_0 = \text{常数}
$$

积分可得相位特性为 $\phi(\omega) = -\tau_0 \omega + \phi_0$。若 $\phi_0 = 0$，则系统满足无失真传输条件——不同频率分量经历相同的时间延迟，信号波形保持不变。

### 群时延变化引起相位失真

当群时延不是常数时，信号的不同频率分量经历不同的延迟时间，导致：

- **包络畸变**：信号的幅度包络形状发生改变
- **脉冲展宽**：窄脉冲通过系统后宽度增加
- **符号间干扰**：相邻符号的能量扩散到彼此的时隙中

### 群时延失真的典型表现

**线性群时延失真**（群时延随频率线性变化）：
- 引起信号包络的对称性展宽
- 对调幅信号的影响表现为包络的不对称畸变

**抛物线群时延失真**：
- 引起脉冲信号的前后沿不对称
- 对调频信号的影响更为严重

## 群时延的测量与计算

### 由相位求群时延

对于给定的相位数据，群时延可以通过数值微分计算：

$$
\tau(\omega_i) = -\frac{\phi(\omega_{i+1}) - \phi(\omega_i)}{\omega_{i+1} - \omega_i}
$$

实际测量中需注意相位解缠（Phase Unwrapping）——相位值被限制在 $(-\pi, \pi]$ 区间，需要恢复连续的相位曲线。

### 由系统函数求群时延

对于有理系统函数 $H(s) = \frac{N(s)}{D(s)}$，群时延可以通过系统函数的零极点计算：

$$
\tau(\omega) = \operatorname{Re}\left[ \sum \frac{1}{j\omega - p_k} - \sum \frac{1}{j\omega - z_k} \right]
$$

其中 $p_k$ 为极点，$z_k$ 为零点。这一表达式说明：
- 靠近虚轴的极点会在对应频率附近引起群时延的尖峰
- 零点也会对群时延产生影响，但效果通常不如极点明显

## 群时延在实际系统中的应用

### 通信系统

在数字通信系统中，群时延的平坦度直接决定系统的误码率：

- **带通滤波器**：滤波器的通带边缘群时延急剧变化，是造成符号间干扰的主要原因
- **信道均衡**：自适应均衡器可以补偿信道的群时延失真
- **OFDM 系统**：对群时延失真比较敏感，需要通过循环前缀和均衡来对抗

### 音频系统

人耳对相位失真相对不敏感，但在高保真音响系统中：

- **扬声器分频网络**：分频器的群时延失真会影响声像定位
- **心理声学**：人耳在 1-4 kHz 范围内对相位失真最敏感

### 图像处理

人眼对相位失真非常敏感：
- 图像处理中的相位失真会导致边缘模糊和几何畸变
- 线性相位（群时延常数）滤波器在图像处理中是首选

### 滤波器设计

在滤波器设计中，群时延的平坦度是仅次于幅频特性的重要指标：

- **贝塞尔滤波器**：通带内群时延最平坦（最大平坦群时延）
- **巴特沃斯滤波器**：群时延随阶数增加而恶化
- **椭圆滤波器**：群时延波动最大

## 群时延与信号速度

群时延对应的速度称为群速度（Group Velocity）：

$$
v_g = \frac{1}{d\beta(\omega)/d\omega}
$$

其中 $\beta(\omega)$ 为传播常数。群速度与相速度（Phase Velocity）的关系：

- 在无色散介质中：$v_g = v_p$
- 在正常色散介质中：$v_g < v_p$
- 在反常色散介质中：$v_g > v_p$，极端情况下可能出现"超光速"现象（但信息速度仍不超过光速）

## 链接到

- 上一个知识点：[5.5 系统物理可实现性](5.5%20%E7%B3%BB%E7%BB%9F%E7%89%A9%E7%90%86%E5%8F%AF%E5%AE%9E%E7%8E%B0%E6%80%A7.md)
- 下一个知识点：[5.7 调制与解调](5.7%20%E8%B0%83%E5%88%B6%E4%B8%8E%E8%A7%A3%E8%B0%83.md)
- 相关知识点：[数字滤波器](06-Z%E5%8F%98%E6%8D%A2/%E6%95%B0%E5%AD%97%E6%BB%A4%E6%B3%A2%E5%99%A8.md)
