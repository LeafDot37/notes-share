---
created: 2026-07-20
tags: [ss-ch1, signals-and-system, analysis-methods]
---

# 系统分析方法

系统分析方法是研究系统对输入信号响应特性的数学工具。根据分析的域和角度不同，可分为不同的方法体系。

---

## 时域法与变换域法

### 时域法（Time-Domain Method）

直接在时间域中分析和求解系统的响应，自变量为时间 $t$。

**基本特点：**
- 物理概念直观，直接反映信号随时间变化的过程
- 数学工具：微分方程（连续系统）、差分方程（离散系统）
- 核心运算：卷积积分（Convolution Integral）

**连续系统时域分析：**
$$
y(t) = f(t) * h(t) = \int_{-\infty}^{\infty} f(\tau) h(t - \tau) d\tau
$$

**离散系统时域分析：**
$$
y[n] = f[n] * h[n] = \sum_{k=-\infty}^{\infty} f[k] h[n - k]
$$

**优点：**
- 物理意义清晰
- 适用于任意输入信号
- 可处理初始条件和非零状态

**缺点：**
- 卷积计算复杂，尤其对于复杂信号
- 对系统特性的刻画不够深刻

### 变换域法（Transform-Domain Method）

将时域信号和系统映射到其他域中进行分析，主要包括：

#### 频率域分析（频域分析）

**工具：傅里叶变换（Fourier Transform）**

$$
F(\omega) = \int_{-\infty}^{\infty} f(t) e^{-j\omega t} dt
$$

**特点：**
- 将信号分解为不同频率的正弦分量
- 系统特性用频率响应 $H(\omega)$ 描述
- 分析信号频谱、带宽、滤波等特性
- 时域卷积 $\to$ 频域乘积：$y(t) = f(t) * h(t) \iff Y(\omega) = F(\omega) H(\omega)$

**适用场景：** 周期信号、能量信号、稳态响应分析

#### 复频率域分析（复频域分析）

**工具：拉普拉斯变换（Laplace Transform）**

$$
F(s) = \int_{0^-}^{\infty} f(t) e^{-st} dt, \quad s = \sigma + j\omega
$$

**特点：**
- 扩展了傅里叶变换，可处理指数增长信号
- 可以自然地包含初始条件
- 系统函数 $H(s)$ 包含全部系统特性
- 稳定性分析：根据 $H(s)$ 的极点位置判断

**适用场景：** 不稳定信号、含初始条件的系统、瞬态响应分析

### 时域法与变换域法的关系

| 特性 | 时域法 | 变换域法 |
|------|--------|----------|
| 自变量 | $t$（时间） | $\omega$（频率）或 $s$（复频率） |
| 系统描述 | $h(t)$（冲激响应） | $H(\omega)$ 或 $H(s)$（系统函数） |
| 运算复杂度 | 卷积运算 | 代数乘法运算 |
| 物理直觉 | 直观 | 频率/复频域视角 |
| 初始条件处理 | 直接 | 需引入初始条件 |

**核心转换关系：**

$$
\text{时域：} y(t) = f(t) * h(t) \quad \xrightarrow{\text{傅里叶变换}} \quad \text{频域：} Y(\omega) = F(\omega) H(\omega)
$$

$$
\text{时域：} y(t) = f(t) * h(t) \quad \xrightarrow{\text{拉普拉斯变换}} \quad \text{复频域：} Y(s) = F(s) H(s)
$$

---

## 输入输出法与状态变量法

### 输入输出法（Input-Output Method）

也称为**外部描述法**，仅关注系统的输入和输出端口之间的关系。

**特点：**
- 将系统视为"黑箱"（Black Box）
- 只描述输入-输出的外部特性
- 数学表示：传递函数 $H(s)$、频率响应 $H(\omega)$、冲激响应 $h(t)$
- 适用于单输入单输出（SISO）系统

**数学工具：**
- 高阶常微分方程
- 传递函数
- 卷积积分

### 状态变量法（State Variable Method）

也称为**内部描述法**，通过引入状态变量来描述系统的内部行为。

**特点：**
- 揭示系统内部结构的动态行为
- 用一阶微分方程组描述（状态方程）
- 适用于多输入多输出（MIMO）系统
- 便于计算机数值求解

**状态方程标准形式：**

$$
\begin{cases}
\dot{\mathbf{x}}(t) = \mathbf{A} \mathbf{x}(t) + \mathbf{B} \mathbf{f}(t) \\
\mathbf{y}(t) = \mathbf{C} \mathbf{x}(t) + \mathbf{D} \mathbf{f}(t)
\end{cases}
$$

其中 $\mathbf{x}(t)$ 是状态向量，$\mathbf{f}(t)$ 是输入向量，$\mathbf{y}(t)$ 是输出向量，$\mathbf{A}, \mathbf{B}, \mathbf{C}, \mathbf{D}$ 为系数矩阵。

### 两种方法的比较

| 特性 | 输入输出法 | 状态变量法 |
|------|-----------|-----------|
| 描述层次 | 外部特性 | 内部结构 |
| 系统类型 | SISO 为主 | MIMO 适用 |
| 方程阶数 | 高阶微分方程 | 一阶微分方程组 |
| 初始条件 | 只含输出初值 | 含所有状态变量初值 |
| 计算适合性 | 手工推导 | 计算机仿真 |

---

## 各分析方法间的关系

### 域与变换的对应

| 域 | 自变量 | 变换工具 | 信号类型 |
|----|--------|----------|----------|
| 时域 | $t$ | — | 所有信号 |
| 频域 | $\omega$（实数频率） | 傅里叶变换 | 能量信号、功率信号 |
| 复频域 | $s = \sigma + j\omega$（复频率） | 拉普拉斯变换 | 所有信号（含指数增长） |

### 分析方法的选择

- **瞬态响应分析** $\to$ 时域法或复频域法（拉普拉斯变换）
- **稳态响应分析** $\to$ 频域法（傅里叶变换）
- **稳定性分析** $\to$ 复频域法（极点位置）或时域法（$h(t)$ 绝对可积性）
- **复杂系统（MIMO）** $\to$ 状态变量法
- **简单系统（SISO）** $\to$ 输入输出法

---

![[Signals and System/images/001_系统分析方法_LTI系统分析方法概览.png]]

## 链接到

- 上一个知识点：[1.5 奇异信号](1.5%20%E5%A5%87%E5%BC%82%E4%BF%A1%E5%8F%B7.md)
- 下一个知识点：[1.7 系统分类与LTI系统](1.7%20%E7%B3%BB%E7%BB%9F%E5%88%86%E7%B1%BB%E4%B8%8ELTI%E7%B3%BB%E7%BB%9F.md)
