---
created: 2026-07-20
tags: [ss-ch6, signals-and-system, discrete-transfer-function]
---

# 离散系统系统函数

系统函数（System Function）$H(z)$ 是描述离散LTI系统输入-输出关系的重要概念。它包含了系统的全部特性：稳定性、因果性、频率响应等。

---

## 系统函数的定义

### 定义方式一：z变换之比

对于离散LTI系统，系统函数定义为系统零状态响应的z变换与激励的z变换之比：

$$
H(z) = \frac{Y_{zs}(z)}{X(z)} = \frac{\mathcal{Z}\{y_{zs}[n]\}}{\mathcal{Z}\{x[n]\}}
$$

### 定义方式二：单位样值响应的z变换

系统函数也是单位样值响应 $h[n]$（Unit Impulse Response）的z变换：

$$
H(z) = \mathcal{Z}\{h[n]\} = \sum_{n=-\infty}^{\infty} h[n] z^{-n}
$$

**核心关系：**

$$
y_{zs}[n] = x[n] * h[n] \quad \xrightarrow{\mathcal{Z}} \quad Y_{zs}(z) = X(z) \, H(z)
$$

---

## 系统函数的求法

### 方法一：由差分方程求 $H(z)$

给定N阶差分方程：

$$
\sum_{k=0}^{N} a_k y[n-k] = \sum_{m=0}^{M} b_m x[n-m]
$$

两边取z变换（零初始条件）：

$$
\sum_{k=0}^{N} a_k z^{-k} Y(z) = \sum_{m=0}^{M} b_m z^{-m} X(z)
$$

$$
H(z) = \frac{Y(z)}{X(z)} = \frac{\sum_{m=0}^{M} b_m z^{-m}}{\sum_{k=0}^{N} a_k z^{-k}}
$$

### 方法二：由单位样值响应求 $H(z)$

若已知 $h[n]$，直接取z变换：

$$
H(z) = \sum_{n=-\infty}^{\infty} h[n] z^{-n}
$$

### 方法三：由系统框图求 $H(z)$

通过系统框图中的基本运算单元（加法器、乘法器、延迟单元 $z^{-1}$）推导：

- 延迟单元 $z^{-1}$ 对应 $H(z) = z^{-1}$
- 串联（级联）：$H(z) = H_1(z) H_2(z)$
- 并联：$H(z) = H_1(z) + H_2(z)$
- 反馈连接：$H(z) = \frac{H_f(z)}{1 \mp H_f(z) H_b(z)}$

---

## 零极点图与系统特性

### 零极点表示

将系统函数表示为因子形式：

$$
H(z) = K \frac{\prod_{m=1}^{M} (z - z_m)}{\prod_{k=1}^{N} (z - p_k)} = K \frac{(z-z_1)(z-z_2)\cdots(z-z_M)}{(z-p_1)(z-p_2)\cdots(z-p_N)}
$$

- $z_m$：零点（Zero），使 $H(z) = 0$ 的 $z$ 值
- $p_k$：极点（Pole），使 $H(z) \to \infty$ 的 $z$ 值
- $K$：增益常数

### 零极点图的应用

零极点分布直观反映了系统的以下特性：

1. **稳定性**：因果系统稳定的充要条件为所有极点位于单位圆内
2. **因果性**：因果系统的ROC在最外极点之外（包括 $\infty$）
3. **频率响应**：通过几何法可以从零极点图估计频率响应
4. **系统类型**：低通、高通、带通、带阻等特性由零极点配置决定

---

## 系统的分类

### FIR系统（Finite Impulse Response）

$$
H(z) = \sum_{n=0}^{M} b_n z^{-n}
$$

- $h[n]$ 为有限长序列
- $H(z)$ 只有零点（极点全在 $z=0$）
- 系统**始终稳定**（极点在单位圆内）
- 可实现线性相位

典型应用：滑动平均滤波器、差分器

### IIR系统（Infinite Impulse Response）

$$
H(z) = \frac{\sum_{m=0}^{M} b_m z^{-m}}{1 + \sum_{k=1}^{N} a_k z^{-k}}
$$

- $h[n]$ 为无限长序列
- $H(z)$ 既有零点也有极点
- 存在稳定性问题（极点必须在单位圆内）
- 相位通常为非线性

典型应用：巴特沃斯滤波器、切比雪夫滤波器

---

## 系统函数与稳定性

### 因果系统稳定性的判断

**充要条件：** 因果系统的所有极点都在单位圆内（$|p_k| < 1, \forall k$）。

**等价条件：** 单位样值响应绝对可和：

$$
\sum_{n=0}^{\infty} |h[n]| < \infty
$$

**几何意义：** $H(z)$ 的ROC包含单位圆（$|z| = 1$ 在ROC内）。

### 极点位置与暂态响应的关系

| 极点位置 | 暂态响应形式 |
|---|---|
| 正实轴 $p>0$ | 单调指数衰减/增长 |
| 负实轴 $p<0$ | 正负交替衰减/增长 |
| 共轭复极点 $p = re^{\pm j\Omega}$ | 正弦衰减振荡（$r<1$） |
| 单位圆上 $|p|=1$ | 临界稳定（等幅振荡） |
| 单位圆外 $|p|>1$ | 不稳定（发散） |

---

## 系统函数与频率响应的关系

系统函数与频率响应的关系为：

$$
H(e^{j\Omega}) = H(z) \big|_{z=e^{j\Omega}}
$$

即频率响应是系统函数在单位圆上的取值。这要求单位圆在ROC内（系统稳定）。

**幅频响应：** $|H(e^{j\Omega})|$
**相频响应：** $\angle H(e^{j\Omega})$

---

## 系统框图的实现

### 直接I型（Direct Form I）

用差分方程直接实现，先实现零点（前馈部分），再实现极点（反馈部分）。

### 直接II型（Direct Form II）

转置直接I型，先实现极点后实现零点，节省延迟单元。

### 级联型（Cascade Form）

将 $H(z)$ 分解为二阶子系统（Biquads）的乘积：

$$
H(z) = \prod_{k=1}^{K} H_k(z)
$$

### 并联型（Parallel Form）

将 $H(z)$ 分解为二阶子系统的和：

$$
H(z) = \sum_{k=1}^{K} H_k(z)
$$

---

## 系统函数的应用

- **系统分析**：由 $H(z)$ 的零极点判断稳定性、因果性和频率特性
- **系统设计**：通过配置零极点实现所需的滤波特性
- **系统实现**：由 $H(z)$ 推导差分方程并构建硬件/软件实现
- **系统仿真**：在频域分析系统对特定输入信号的响应

## 链接到

- 上一个知识点：[6.7 离散时间傅里叶变换DTFT](6.7%20%E7%A6%BB%E6%95%A3%E6%97%B6%E9%97%B4%E5%82%85%E9%87%8C%E5%8F%B6%E5%8F%98%E6%8D%A2DTFT.md)
- 下一个知识点：[6.9 离散系统频率响应](6.9%20%E7%A6%BB%E6%95%A3%E7%B3%BB%E7%BB%9F%E9%A2%91%E7%8E%87%E5%93%8D%E5%BA%94.md)
- 相关知识点：[系统函数H(s)](04-%E6%8B%89%E6%99%AE%E6%8B%89%E6%96%AF%E5%8F%98%E6%8D%A2/%E7%B3%BB%E7%BB%9F%E5%87%BD%E6%95%B0H%28s%29.md)、[系统函数的频域分析](05-%E9%A2%91%E5%9F%9F%E5%88%86%E6%9E%90/%E7%B3%BB%E7%BB%9F%E5%87%BD%E6%95%B0%E7%9A%84%E9%A2%91%E5%9F%9F%E5%88%86%E6%9E%90.md)

[[06-Z变换/index|← 返回 第6章 Z变换]]
