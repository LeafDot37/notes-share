---
tags: [ss-ch2, signals-and-system, convolution-properties]
---

# 卷积性质

## 基本代数性质

卷积运算满足以下三个基本代数律，这些性质源于积分运算的线性性。

### 交换律

卷积运算的顺序可以交换：
$$ f_1(t) * f_2(t) = f_2(t) * f_1(t) $$

证明：
$$ f_1(t) * f_2(t) = \int_{-\infty}^{\infty} f_1(\tau) f_2(t - \tau) d\tau $$
令 $u = t - \tau$，则 $\tau = t - u$，$d\tau = -du$：
$$ = -\int_{\infty}^{-\infty} f_1(t - u) f_2(u) du = \int_{-\infty}^{\infty} f_2(u) f_1(t - u) du = f_2(t) * f_1(t) $$

**意义**：在进行卷积计算时，可以选择将较简单的信号作为翻褶对象，简化计算。

### 分配律

卷积对加法满足分配律：
$$ f_1(t) * [f_2(t) + f_3(t)] = f_1(t) * f_2(t) + f_1(t) * f_3(t) $$

证明直接由积分线性性得到。

**意义**：并联系统的总冲激响应等于各子系统冲激响应之和。

### 结合律

卷积运算满足结合律：
$$ [f_1(t) * f_2(t)] * f_3(t) = f_1(t) * [f_2(t) * f_3(t)] $$

**意义**：级联（串联）系统的总冲激响应等于各子系统冲激响应的卷积。

## 与冲激函数的卷积

冲激函数是卷积运算中的**单位元**：

$$ f(t) * \delta(t) = f(t) $$

**证明**：
$$ f(t) * \delta(t) = \int_{-\infty}^{\infty} f(\tau) \delta(t - \tau) d\tau = f(t) $$

推广形式：

1. **延时冲激**：
   $$ f(t) * \delta(t - t_0) = f(t - t_0) $$
   信号与延时冲激卷积，相当于信号本身发生相同延时。

2. **冲激导数**：
   $$ f(t) * \delta'(t) = f'(t) $$
   信号与冲激的一阶导数卷积，得到信号的导数。

3. **高阶冲激导数**：
   $$ f(t) * \delta^{(n)}(t) = f^{(n)}(t) $$

4. **冲激函数与系统冲激响应**：
   $$ h(t) * \delta(t) = h(t) $$

## 与阶跃函数的卷积

$$ f(t) * u(t) = \int_{-\infty}^{t} f(\tau) d\tau $$

信号与阶跃函数卷积，相当于对信号进行积分运算。

## 时移性质

若 $f_1(t) * f_2(t) = f(t)$，则：
$$ f_1(t - t_1) * f_2(t - t_2) = f(t - t_1 - t_2) $$

即两个信号分别延时后的卷积，等于原卷积结果延时两个延时量之和。

**证明**：
令 $f_1(t - t_1) * f_2(t - t_2)$，利用延时冲激的卷积性质：
$$ f_1(t - t_1) * f_2(t - t_2) = [f_1(t) * \delta(t - t_1)] * [f_2(t) * \delta(t - t_2)] $$
$$ = f_1(t) * f_2(t) * \delta(t - t_1 - t_2) = f(t - t_1 - t_2) $$

## 微分性质

若 $f(t) = f_1(t) * f_2(t)$，则：
$$ \frac{d f(t)}{dt} = \frac{d f_1(t)}{dt} * f_2(t) = f_1(t) * \frac{d f_2(t)}{dt} $$

**证明**：
$$ \frac{d}{dt} \left[ f_1(t) * f_2(t) \right] = \frac{d}{dt} \int_{-\infty}^{\infty} f_1(\tau) f_2(t - \tau) d\tau $$
$$ = \int_{-\infty}^{\infty} f_1(\tau) \frac{\partial}{\partial t} f_2(t - \tau) d\tau = f_1(t) * \frac{d f_2(t)}{dt} $$

由交换律可得另一个等价形式。

**推广**：
$$ f^{(n)}(t) = f_1^{(k)}(t) * f_2^{(n-k)}(t) \quad (0 \leq k \leq n) $$

## 积分性质

若 $f(t) = f_1(t) * f_2(t)$，则：
$$ \int_{-\infty}^{t} f(\tau) d\tau = \left[ \int_{-\infty}^{t} f_1(\tau) d\tau \right] * f_2(t) = f_1(t) * \left[ \int_{-\infty}^{t} f_2(\tau) d\tau \right] $$

即卷积的积分等于其中一个信号的积分与另一个信号的卷积。

## 微分积分性质

综合微分与积分性质：

$$ f(t) = f_1(t) * f_2(t) = \frac{d f_1(t)}{dt} * \int_{-\infty}^{t} f_2(\tau) d\tau = \int_{-\infty}^{t} f_1(\tau) d\tau * \frac{d f_2(t)}{dt} $$

**应用**：利用微分积分性质，可以将一个信号的微分与另一个信号的积分进行卷积，简化分段信号的卷积计算。例如，一个矩形脉冲可以表示为阶跃信号的微分，而阶跃信号与任意信号的卷积就是该信号的积分。

## 卷积的展宽性质

若 $f_1(t)$ 的非零区间长度为 $L_1$，$f_2(t)$ 的非零区间长度为 $L_2$，则 $f_1(t) * f_2(t)$ 的非零区间长度为 $L_1 + L_2$。

更精确地，若 $f_1(t)$ 在 $[a_1, b_1]$ 外为零，$f_2(t)$ 在 $[a_2, b_2]$ 外为零，则卷积在 $[a_1 + a_2, b_1 + b_2]$ 外为零。

## 性质总结表

| 性质 | 公式 |
|---|---|
| 交换律 | $f_1 * f_2 = f_2 * f_1$ |
| 分配律 | $f_1 * (f_2 + f_3) = f_1 * f_2 + f_1 * f_3$ |
| 结合律 | $(f_1 * f_2) * f_3 = f_1 * (f_2 * f_3)$ |
| 单位元 | $f * \delta = f$ |
| 时移 | $f_1(t-t_1) * f_2(t-t_2) = f(t-t_1-t_2)$ |
| 微分 | $(f_1 * f_2)' = f_1' * f_2 = f_1 * f_2'$ |
| 积分 | $\int (f_1 * f_2) = (\int f_1) * f_2 = f_1 * (\int f_2)$ |

## 链接到

- 上一个知识点：[2.1 冲激响应与阶跃响应](2.1%20%E5%86%B2%E6%BF%80%E5%93%8D%E5%BA%94%E4%B8%8E%E9%98%B6%E8%B7%83%E5%93%8D%E5%BA%94.md)
- 下一个知识点：[2.3 卷积概念与图解](2.3%20%E5%8D%B7%E7%A7%AF%E6%A6%82%E5%BF%B5%E4%B8%8E%E5%9B%BE%E8%A7%A3.md)
- 相关知识点：
