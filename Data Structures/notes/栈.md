# 栈

## 1

栈是一种数据结构，其顺序存储结构由data、top、size组成，data指一个数组，存有各数据，top为栈顶元素所在位置，size指数组大小，便于动态更新；链式存储结构由节点内容、下一个节点组成，通过指向栈顶元素的top指针更新栈。

## 2

栈的基本规定是先进后出。函数的调用等用的都是栈顺序，当调用次数过多时可以用栈来防止内存溢出。

## 3

栈的基本操作有init、destroy、isempty、lenth、clear、push、pop、gettop等等。

## 4

在C++中stl内有设计好的栈对象，可以直接调用使用。
