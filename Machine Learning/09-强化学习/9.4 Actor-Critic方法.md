---
tags: [ml-ch9, machine-learning, 强化学习, Actor-Critic, 策略梯度]
created: 2026-07-20
---

# Actor-Critic方法

Actor-Critic方法是一种结合了基于价值学习和基于策略学习的强化学习算法。它同时维护策略函数（Actor）和价值函数（Critic），利用两者的优势实现更加稳定和高效的学习。

## 基本框架

Actor-Critic方法包含两个核心组件：

### Actor（演员）

Actor 是策略函数 $\pi(a|s)$，负责根据当前状态选择动作。它通过策略梯度方法更新参数，目标是最大化期望累积回报。

### Critic（评论家）

Critic 是价值函数 $V(s)$ 或 $Q(s,a)$，负责评估 Actor 所选动作的质量。Critic 通过时间差分（TD）学习来估计状态或动作的价值，并为 Actor 提供反馈信号。

## 更新过程

在每一步交互中，Agent 执行以下更新：

1. Actor 根据当前状态 $s$ 选择动作 $a$
2. 环境返回奖励 $r$ 和下一状态 $s'$
3. Critic 计算 TD 误差（优势函数）：

$$
\delta = r + \gamma V(s') - V(s)
$$

其中 $\gamma$ 是折扣因子，$\delta$ 反映了当前动作相对于平均水平的优劣。

4. Critic 更新其价值函数参数以缩小 TD 误差：

$$
\theta_V := \theta_V + \alpha_V \cdot \delta \cdot \nabla_{\theta_V} V(s)
$$

5. Actor 利用 Critic 提供的 TD 误差更新策略参数：

$$
\theta_\pi := \theta_\pi + \alpha_\pi \cdot \delta \cdot \nabla_{\theta_\pi} \log \pi(a|s)
$$

## 优势与挑战

**优势**：
- 相比纯策略梯度方法，Critic 提供的价值估计降低了策略梯度的方差，使训练更加稳定
- 相比纯价值学习方法，Actor 直接优化策略，适用于连续动作空间
- 可以处理随机策略和确定性策略

**挑战**：
- Actor 和 Critic 的联合训练可能不稳定，需要精心协调学习率
- 价值函数估计可能存在偏差，影响策略更新的方向
- 在实际应用中常使用优势函数（Advantage Function）$A(s,a) = Q(s,a) - V(s)$ 进一步降低方差

## 代表性算法

基于 Actor-Critic 框架的代表算法包括 A2C（Advantage Actor-Critic）、A3C（Asynchronous Advantage Actor-Critic）、以及近年广泛使用的 PPO（Proximal Policy Optimization）和 SAC（Soft Actor-Critic）等。

## 链接到

- 上一个知识点：[9.3 基于策略的学习](9.3%20%E5%9F%BA%E4%BA%8E%E7%AD%96%E7%95%A5%E7%9A%84%E5%AD%A6%E4%B9%A0.md)
- 下一个知识点：无（本章最后一个知识点）
- 相关知识点：[基于人类反馈的强化学习](11-%E5%A4%A7%E8%AF%AD%E8%A8%80%E6%A8%A1%E5%9E%8B%E5%85%B3%E9%94%AE%E6%8A%80%E6%9C%AF/%E5%9F%BA%E4%BA%8E%E4%BA%BA%E7%B1%BB%E5%8F%8D%E9%A6%88%E7%9A%84%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0.md)

[[index|← 返回 第9章 强化学习]]
